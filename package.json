{
  "name": "screenshot-service",
  "version": "1.0.0",
  "description": "Serverless screenshot capture service.",
  "main": "api/capture.js",
  "scripts": {
    "vercel-build": "npm install && npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "@sparticuz/chromium": "^123.0.1",
    "playwright": "^1.44.1",
    "playwright-aws-lambda": "^0.11.0",
    "playwright-core": "^1.50.1"
  }
}
